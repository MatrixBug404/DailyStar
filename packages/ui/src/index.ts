/**
 * @dailystar/ui
 *
 * Shared design-system component library for the DailyStar platform.
 * Components are added phase by phase alongside their consuming features.
 *
 * Phase 5+: Public site components (ArticleCard, CategoryNav, SearchBar, etc.)
 * Phase 6+: CMS components (Editor, ReviewQueue, WorkflowStatus, etc.)
 *
 * All components must:
 * - Be styled with Tailwind CSS
 * - Be fully typed with TypeScript
 * - Have no business logic (pure presentational / UI)
 * - Be independently testable with React Testing Library
 */

// Components will be exported from here as they are built.
// Example (future):
//   export { Button } from './components/Button';
//   export { Badge } from './components/Badge';
